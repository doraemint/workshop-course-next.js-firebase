import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  outputFileTracingRoot: '/Users/tanats/Desktop/project/my/nak-dev/next.js+firebase',
};

export default nextConfig;
